@model ABCMusic_Auth.Models.AdminViewModels.RoleViewModel

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>RoleViewModel</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
	<input type="button" class="btn update-button" value="Edit"
		   onclick="location.href='@Url.Action("Edit", "UserRoles", new { id = Model.Id })'" />
	<input type="button" class="btn delete-button" value="Delete"
		   onclick="location.href='@Url.Action("Delete", "UserRoles", new { id = Model.Id })'" />
	<input type="button" class="btn btn-primary" value="Roles"
		   onclick="location.href='@Url.Action("Index", "UserRoles", new { })'" />
</div>
